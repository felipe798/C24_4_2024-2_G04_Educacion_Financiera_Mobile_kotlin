<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#121212">

    <!-- Título para proporcionar contexto a la pantalla -->
    <TextView
        android:id="@+id/tvModuleTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Módulos del Curso"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- RecyclerView para mostrar los módulos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerModules"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#1E1E1E"
        android:padding="8dp"
        android:scrollbars="vertical"
        android:elevation="2dp"
        android:clipToPadding="false" />

    <!-- Botón para ver el examen -->
    <Button
        android:id="@+id/btnExam"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Examen no disponible"
        android:layout_marginTop="16dp"
        android:background="#03A9F4"
        android:textColor="#FFFFFF"
        android:padding="12dp"
        android:backgroundTint="#2196F3"
        android:elevation="4dp"
        android:layout_marginBottom="8dp"

        android:drawablePadding="8dp"
        android:gravity="center" />

</LinearLayout>
